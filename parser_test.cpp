#include <iostream>
#include <map>
#include "src/Token.h"
#include "Parser.h"

using namespace std;

int main()
{
    Parser p;
    Token t;

    /*
    while(t = getToken(stream)) {
        p.parse(t);
    }
    p.parse(); // pass end-of-input token
    */

    return 0;
}
